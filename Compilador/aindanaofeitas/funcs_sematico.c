#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "tabela_simbolos.h"
#include "semantico.h"



// void inserttree(pont_arv arv, tabelaSimbolos *hashtable, char *escopo) {
//     if (arv != NULL) {
//         if (arv->tipo == 1) {
//             inserttable(hashtable, arv->typeID, arv->dataType, arv->nameID, escopo, arv->linha);
//         }
//         inserttree(arv->esq, hashtable, escopo);
//         inserttree(arv->dir, hashtable, escopo);
//     }

// }
